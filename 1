import random
def guess(x):
    random_number = random.randint(1, x)
    guess = 0
    whle guess != random_number:
    guess = input(f'guess a number between 1 and {x}: ')
    print(guess)
   
guess(10)
